# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Next.js build output
/.next/
/out/
# Alternate build dirs used via NEXT_DIST_DIR (see next.config.ts) so a second
# Next process can run without clobbering a running server's .next
/.next-*/

# Production
/build

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local environment files (NEVER COMMIT SECRETS)
.env*.local
.env.development.local
.env.test.local
.env.production.local
.env

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# OS Files
.DS_Store
*.pem
Thumbs.db
